name: ci

on:
  pull_request:
  push:
    branches:
      - "master"

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Build Workflow
        uses: elle-Bee/handscape/.github/workflows/build.yaml@main
